from setuptools import setup, find_packages

setup(
    name='PATH',
    version='',
    description='HumanBench',
    author='',
    author_email='',
    packages=find_packages(),
    install_requires=[
    ],
)