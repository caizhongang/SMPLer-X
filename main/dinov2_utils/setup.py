from setuptools import setup, find_packages

setup(
    name='dinov2',
    version='',
    description='dinov2',
    author='',
    author_email='',
    packages=find_packages(),
    install_requires=[
    ],
)