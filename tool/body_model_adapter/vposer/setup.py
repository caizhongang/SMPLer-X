from setuptools import setup, find_packages

setup(
    name='human_body_prior',
    version='',
    description='',
    author='',
    author_email='',
    packages=find_packages(),
    install_requires=[
    ],
)
